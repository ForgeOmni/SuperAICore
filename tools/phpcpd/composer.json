{
    "description": "Isolated install of phpcpd so its phpunit/php-timer pin never constrains the package's own test matrix (phpcpd 7 needs php-timer ^6.0, phpunit >= 11.5 needs ^7.0). Install with: composer run duplication:install",
    "require": {
        "php": "^8.1",
        "systemsdk/phpcpd": "^7.0"
    },
    "config": {
        "allow-plugins": false
    }
}
