#!/usr/bin/env php
<?php

/**
 * ai-core — unified CLI for calling any configured backend.
 *
 * Examples:
 *   ai-core call "Hello" --backend=anthropic_api --model=claude-sonnet-4-5-20241022
 *   ai-core call "Hello" --backend=claude_cli
 *   ai-core call "Hello" --backend=codex_cli
 *   ai-core call "Hello" --backend=superagent
 *   ai-core list-backends
 */

$autoloadCandidates = [
    __DIR__ . '/../vendor/autoload.php',        // when developing ai-core
    __DIR__ . '/../../../autoload.php',         // when installed as dependency
    __DIR__ . '/../../../../vendor/autoload.php',
];

foreach ($autoloadCandidates as $candidate) {
    if (file_exists($candidate)) {
        require $candidate;
        break;
    }
}

use ForgeOmni\AiCore\Console\Application;

$app = new Application();
$app->run();
