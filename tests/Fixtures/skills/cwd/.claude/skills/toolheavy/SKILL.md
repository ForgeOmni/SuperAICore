---
name: toolheavy
description: Exercises translator and probe paths (Agent + file + web tools).
---
Use the `Read` tool to open files, then use `Write` to save a summary.
Call `Agent` to spawn a sub-researcher.
Call `WebSearch` when you need external facts.
